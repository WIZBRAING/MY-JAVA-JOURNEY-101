/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt 
to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this
template
 */
package exceptionhandling;
import java.util.InputMismatchException;
import java.lang.Throwable;
import java.util.IllegalFormatException;
import java.util.Scanner;
import java.text.DecimalFormat;
import java.io.IOException;
import java.io.File;
import java.io.FileNotFoundException;
import java.util.Arrays;
import javax.swing.JFrame;
import java.nio.file.Path;    //for getting path in the new input and output system.
import java.nio.file.Files;  //same as above but for files. It's supported by the new file system.
import java.net.URL;
import java.net.MalformedURLException;
import java.util.ArrayList;
import java.io.PrintWriter;

/**
 *
 * @author WIZBRAIN
 */

public class ExceptionHandling {

    /**
     * @param args the command line arguments
     */

    
    public static void main(String[] args) {
        // TODO code application logic here
        //creatingFile();  
        Exercises ex = new Exercises();
        //File file = new File("files/Exercise12_15.txt");
        File file = new File("files/removeText.txt");
        //ex.count_L_W_C(file);
        //ex.getScores();
        //ex.readOrWriteData();
        //ex.replaceText(" line", file);
        //Methods.crawl("http://cs.armstrong.edu/liang");
        //ex.hangman();
        //ex.countWords();
        //ex.createFiles();
        //ex.addPackageStatement();
        //ex.createFiles();
        //ex.addPackageStatement();
        //ex.removePackageStatement();
        //ex.sortedString();
        //ex.removeFiles();
        //ex.ReplaceText();
        //ex.getTotalScore();
        //ex.createDataset();
        //ex.processDataSet();
        //ex.createDirctory();
        //ex.replaceWords();
        //ex.renameFiles_();
        //ex.occurenceOfEachLetters();
    }  //END OF MAIN METHOD 


} //END OF CLASS
